`use strict`;
const defaultSettingsFilename = `./settings/default/default-settings.txt`;
const tokenFilename = `./token.txt`;
const wavFilename = `sam.wav`;
let defaultSettings = [];
const debugMode = true;

module.exports = {defaultSettings, defaultSettingsFilename, tokenFilename, debugMode, wavFilename};
