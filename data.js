`use strict`;
const defaultSettingsFilename = `./settings/default/default-settings.txt`;
const tokenFilename = `./token.txt`;
const debugMode = true;

module.exports = {defaultSettingsFilename, tokenFilename, debugMode};
